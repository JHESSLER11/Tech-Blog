<article class="post">
    <div class="title">
        <a href="/post/{{id}}" target="_blank">{{title}}</a>
        {{!-- <span>({{format_url contents}})</span> --}}
    </div>
  <div class="meta">
    Post by {{user.username}} on {{created_at}} on {{format_date created_at}}
    {{!-- <a href="/post/{{id}}">{{comments.length}} comment(s)</a> --}}
  </div>
    <div>
      <p>{{contents}}</p> 
    </div>
</article>